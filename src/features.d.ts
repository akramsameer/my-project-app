declare module "@features" {
  import type { ComponentType } from "react";
  export const Analytics: ComponentType | null;
  export const ProBadge: ComponentType | null;
  export const isPro: boolean;
}
