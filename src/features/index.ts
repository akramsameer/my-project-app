// Open-source edition stubs
// When @my-project/pro is installed, these are swapped automatically
// via the webpack alias in next.config.mjs

import type { ComponentType } from "react";

export const Analytics: ComponentType | null = null;
export const ProBadge: ComponentType | null = null;
export const isPro = false;
